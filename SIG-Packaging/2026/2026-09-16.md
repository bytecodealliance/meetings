# 2026 September 16 - SIG-Packaging meeting

# Agenda

1. `wkg` 0.17.0 topics
1. PRs and Issues

## Attendees

* Mikhail
* Victor

# Notes

## feat(core,wkg): resolve multiple versions of the same package
* https://github.com/bytecodealliance/wasm-pkg-tools/pull/251

Mikhail:
 * My concern and why I proposed the graph approach is to eliminate possibilities that overrides can overlap/overwrite resolutions
 * Should I submit a PR on top of Aditya's?

Victor:
 * I will reach out to Aditya to see if we can push directly to his branch
 * Initially create a test case that demonstrates the gap you are discussing and we will go from there

## Add capability to specify cache directory for registry

https://github.com/bytecodealliance/wasm-pkg-tools/issues/249

Mikhail:
 * digest cache reads/writes under `~/.cache/wasm-pkg/` currently resolves under only namespace + pkg name:
   `~/.cache/wasm-pkg/wasi:http-0.2.0.json`
 * how do we handle aliases and local backends?

Victor:
 * that's definitely a gap, we could use double underscores to delimit registry + backend type:
  ```
  CACHE_DIR/acme_registry_com__local
  override
  namespace
  cache
  ```
 * we can add a suffix to the registry to include backend
 * `~/.cache/wasm-pkg/ghcr.io__wasi:http-0.2.0.json`
